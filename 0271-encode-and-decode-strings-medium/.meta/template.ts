/**
 * String Encode and Decode - Medium
 * 
 * https://neetcode.io/problems/string-encode-decode
 * 
 * Design an algorithm to encode a list of strings to a single string. 
 * The encoded string is then decoded back to the original list of strings.
 * 
 * Example 1:
 * Input: ["neet","code","love","you"]
 * Output: ["neet","code","love","you"]
 * 
 * Example 2:
 * Input: ["we","say",":","yes"]
 * Output: ["we","say",":","yes"]
 * 
 * Constraints:
 * - 0 <= strs.length < 100
 * - 0 <= strs[i].length < 200
 * - strs[i] contains only UTF-8 characters.
 */

export class Codec {
    // Encode a list of strings to a single string
    // Each string is encoded as "<length>#<string>", and all strings are concatenated
    encode(strs: string[]): string {
        // 🚀 Let's do this! Time to turn a list of strings into a single encoded string.
        return '';
    }

    // Decode a single string to a list of strings
    decode(s: string): string[] {
        // 🧠 Stay sharp — edge cases might sneak in, but you're smarter.
        return [];
    }
}
